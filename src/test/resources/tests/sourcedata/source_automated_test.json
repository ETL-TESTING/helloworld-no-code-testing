{
    "scenarioName": "SOURCE AUTOMATED TEST",
    "steps": [
        {
            "name": "source_record_count",
            "url": "db://MagicExecutor",
            "operation": "EXECUTE",
            "request": {
                "sql": "${SQL.FILE:sql/source_count.sql}"
            },
            "verify": { }
        }
    ]
}