{
    "scenarioName": "TARGET AUTOMATED TEST @Ben",
    "steps": [
        {
            "name": "target_record_count",
            "url": "db://MagicExecutor",
            "operation": "EXECUTE",
            "request": {
                "sql": "${GQL.FILE:sql/target_count.sql}"
            },
            "verify": { }
        }
    ]
}